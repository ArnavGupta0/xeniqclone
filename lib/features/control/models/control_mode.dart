/// Control mode enum for switching between Button and Gyroscope modes
enum ControlMode {
  /// Button mode - zoom slider, reset zoom, point object
  button,
  
  /// Gyroscope mode - orientation guidance via dots
  gyroscope,
}
